import { GUESS } from "../actions/types"
const x = 'http://www.justhangman.com/images/hangman_0_thanksgiving.gif'

export default (state = x, { type, payload } ={}) => {
  switch (type) {
    case GUESS :
    return
      state

      default :
      return state
    }
    }
